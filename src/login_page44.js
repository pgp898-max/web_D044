import React from 'react';

const LoginPage = () => {
  return (
    <div>
      <h2>Login Page</h2>
      <input type="text" placeholder="Username" />
      <button>Login</button>
    </div>
  );
};

export default LoginPage;