import React from 'react'; // 1. Import the React tool

function MyFile() {       // 2. Create the function (The Component)
  return (
    <div>
      <h1>Hello GitHub!</h1>
      <p>This is my first React file.</p>
    </div>
  );
}

export default MyFile;    // 3. Export it so other files can use it